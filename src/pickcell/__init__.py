"""The shared pick pipeline: detection, measurement, obstacles, poses and the
``PickPipeline`` orchestration, importable by both ``examples/pick_red_block.py``
and (phase 4) the conductor model. Consumers import submodules directly; this
package carries no barrel re-exports."""
